services:
  api:
    build: .
    ports:
      - "8000:8000"
  

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: patrik
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5432:5432"
      